%h1 Listing events
%br
Tapahtumia: #{Event.count}
%br
%br

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @events.each do |event|
    %tr
      %td= event.name + " (" + get_series(event.series) + ")"
      -# - @asdf = event.sport
      -# - @asdf.events.each do |moro|
      -#   = @asdf.name + ": " + moro.name

      %td= link_to 'Show', event
      %td= link_to 'Edit', edit_event_path(event)
      %td= link_to 'Destroy', event, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Event', new_event_path
%br
%br
-# - @events.each do |event|
-#   = "{:name => '" + event.name + "', :sport => Sport.find(" + event.sport_id.to_s + "), :series => " + event.series.to_s + "},"
-#   %br
